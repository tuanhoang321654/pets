describe('Product Service', () => {
  beforeEach(async () => {});

  it('Cannot create if product is null', () => {});

  it('Create a product success', () => {});

  it('Cannot Delete a product if id is not found', () => {});

  it('Cannot Delete a success', () => {});
});
